# neural-vs-classical
RL agents (Q-learning, SARSA) learn to solve mazes from scratch, racing live against classical search algorithms (BFS/DFS/Dijkstra/A*). C-powered simulation core, FastAPI + WebSocket backend, animated React frontend. Includes adversarial maze generation and live Q-value explainability.
